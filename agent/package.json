{
  "name": "aura-architect",
  "version": "0.1.0",
  "private": true,
  "description": "AI architect pipeline: questionnaire -> design brief -> Alberta budget -> escrow milestones",
  "author": "Matt Aurora Ventures <lucidbloks@gmail.com>",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "demo": "npm run build && node dist/index.js",
    "brain": "npm run build && node dist/brain/run.js",
    "memory": "npm run build && node dist/brain/memory-demo.js",
    "mcp": "npm run build && node dist/mcp/server.js",
    "mcp:paid": "npm run build && node dist/mcp/server.js --paid",
    "mcp:smoke": "npm run build && node dist/mcp/smoke.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
