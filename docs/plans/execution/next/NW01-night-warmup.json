{
  "node": "NW01",
  "graphVersion": "1.2",
  "job": "Eliminate the two measured one-time compile stalls: ~5.7 s after the full-tier promotion and ~6.7 s on the first night toggle, via asynchronous program warmup.",
  "context": "meadow-proof LoAF ledgers attribute both stalls to FrameRequestCallback in the scene chunk right after settle and on the first 'Switch to night' click - synchronous WebGL program compilation of the restored full-tier set (EffectComposer, PCSS) and the night material set. The scene already has the pattern to copy: GrassProgramWarmup in app/components/story/SceneDetail.tsx uses gl.compileAsync(warmScene, camera) behind KHR_parallel_shader_compile.",
  "writeSet": [
    "app/components/story/Scene.tsx",
    "app/components/story/SceneDetail.tsx",
    "app/tests/meadow-progressive.spec.ts",
    "app/scripts/meadow-proof.mjs (budget tightening only, after the fix proves out)"
  ],
  "rejectionGates": [
    "never compile synchronously on mount - the warmup must be idle-scheduled AFTER settled, or it recreates the stall it removes",
    "the warmup must not mount visible geometry or lights that change the image (three.js recompiles ALL programs when the light count changes - warm with the real light configuration)"
  ],
  "diagnostics-2026-08-14": {
    "confirmed": [
      "Stalls persist in the latest passing proof: ~5.4 s long task at ~25 s (post-settle full-tier promotion: EffectComposer + SoftShadows PCSS + IBL + rich materials mounting together - PCSS patches shader chunks globally and Environment changes envMap defines, so the whole material set recompiles), and ~6.8 s at ~31 s (night-switch wave).",
      "LightArc uses ONE directional light repositioned day/dusk/night - no light-count change there.",
      "OutdoorLighting's pointLight and Bollards are always mounted, intensity-scaled - not the trigger.",
      "StarField's `amount < 0.02 -> null` compiled its program on the first night click - FIXED Aug 14 (always mounted, opacity-faded).",
      "The 6.8 s night trigger is NOT yet attributed - no obvious conditional light/material at the Scene level; FirePit/HotTub/Props internals or a fog/define flip remain suspects."
    ],
    "strategySketch": "The durable fix is compile-at-full-complexity-behind-the-loader: mount SoftShadows, the composer (effects at zero intensity), and Environment from first paint on machines that will earn full, making tier promotion pure runtime state (uniforms/resolution) instead of mounts. That moves ALL compile cost behind the loader the stage machine already owns. Requires care on weak machines (they never promote, so gate the early mounts on the same lowPower check)."
  },
  "instructions": [
    "1. FIRST attribute the 6.8 s night wave precisely: temporary harness that wraps THREE.WebGLRenderer.compile/onBeforeCompile with console timing (or a Spector.js capture) across one night toggle, listing every program compiled and its ms. Do not guess.",
    "2. Build a NightProgramWarmup component modeled on GrassProgramWarmup: after the meadow reports terminal (listen to aura:meadow-progress like StoryCanvas does) and behind requestIdleCallback, gl.compileAsync a hidden scene containing the night-only materials with the night-time light configuration.",
    "3. Measure with scripts/meadow-proof.mjs on a quiet machine: interactionMs (first toggle) should drop toward the steady 150 ms range; the post-settle LoAF burst should disappear from fullRunLongAnimationFrames.",
    "4. Once proven across 2 consecutive runs, tighten the proof budget: interactionMs <= 250 back down toward 160, and note the change in the proof comment.",
    "5. Spec-pin the warmup existence in meadow-progressive.spec.ts (grep for the component name + compileAsync)."
  ],
  "verify": {
    "commands": ["npx tsc --noEmit", "npm test", "npm run test:ui", "AURA_MEADOW_BASELINE=<baseline-dir> node scripts/meadow-proof.mjs"],
    "note": "two consecutive passing proofs required before tightening the budget"
  },
  "sideEffect": "local",
  "status": "ready"
}
