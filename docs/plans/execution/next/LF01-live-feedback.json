{
  "node": "LF01 + LF02",
  "graphVersion": "1.2",
  "job": "Cost bands and constraint checks that move WHILE the design changes, plus one honest readiness reading that separates design intent from ready-for-professional-review.",
  "context": "createProjectBudget (lib/builder/projectBudget.ts) already recomputes from the document and carries a confidence score, gaps, and a cap comparison - but it lives on /budget, so a person editing the home cannot see the consequence of the edit they are making. checkSpecAgainstParcel now answers the fit question in the Site step. The pieces exist; they are just not where the decision is.",
  "writeSet": [
    "app/components/builder/LiveReadout.tsx (new)",
    "app/components/builder/BuilderApp.tsx",
    "app/lib/builder/readiness.ts (new)",
    "app/tests/builder-readout.spec.ts (new)",
    "app/tests/project-budget.spec.ts"
  ],
  "rejectionGates": [
    "the live bands MUST come from createProjectBudget - a second, parallel cost calculation in the editor is exactly the divergence class that has bitten this repo twice (mask literals, slope). One source or none.",
    "agent/ npm run demo must still reconcile the anchored triplet to the dollar afterwards",
    "the readiness reading must be falsifiable: a design missing a required input CANNOT read as ready. Prove it by constructing one in the spec and asserting it reads not-ready."
  ],
  "instructions": [
    "1. lib/builder/readiness.ts: a pure function from (document, budget, parcel check) to { state: 'design-intent' | 'review-ready', gaps: string[] }. Every gap names the specific missing input and where to supply it. No score theatre - a number that cannot be acted on is decoration.",
    "2. LiveReadout: a quiet strip in the builder showing the LOW/MID/HIGH band, the two or three constraint checks that currently apply (minimum dwelling size, buildable-envelope fit, FDWR where modelled), and the readiness state. It updates as the document changes and animates only within the 150-250 ms motion tokens.",
    "3. Every figure must trace to its module and say what it is: a planning range, not a quote; a modelled check, not a permit decision.",
    "4. Specs: editing the design changes the band; removing a required input flips readiness to design-intent and names the gap; the readiness function is pure and deterministic."
  ],
  "verify": {
    "commands": ["npx tsc --noEmit", "npm test", "cd ../agent && npm run demo"],
    "note": "the money anchor is the hard gate"
  },
  "sideEffect": "local",
  "status": "verified",
  "evidence": {
    "verifiedAt": "2026-08-14",
    "closedLate": "Shipped in b36b222; sat at 'ready' until Audit #9 finding 2.",
    "delivered": "lib/builder/readiness.ts (pure, deterministic) plus a LiveReadout strip in the builder carrying the LOW/MID/HIGH band, the constraint checks that currently apply, and one readiness reading that separates design intent from ready-for-professional-review with the specific gaps named.",
    "rejectionGateHeld": "The bands come from createProjectBudget and nowhere else. No second cost path was written - which matters, because a parallel calculation of one fact is the divergence class that produced grass through a deck and a drawing contradicting its owner. `agent/ npm run demo` re-verified: 199,100 / 301,280 / 443,900 to the dollar.",
    "honestyBehaviour": "For a graph-geometry design the readout reports the setback check as 'Not run' and the glazing ratio as 'Not modelled', with the reason, rather than showing a plausible number for a check it did not perform. That behaviour surfaced a strict-mode collision in graph-ui.spec (one truthful disclosure now appears three times); the assertion was renegotiated to name the export tab's own wording rather than taking .first(), which would have passed while asserting nothing.",
    "openFinding": "Audit #9 finding 10 (LOW): the coverage detail gives a stale instruction in graph mode. Being corrected in the audit-9 remediation wave.",
    "gates": "tsc clean; builder-readout spec; 290 unit; 63 UI."
  }
}
