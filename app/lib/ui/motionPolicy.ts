/** Motion policy for practical project routes. The landing story keeps its
 * cinematic timing; task-focused routes favor immediate input response. */
export const APP_SCROLL_BEHAVIOR = "native" as const;
export const APP_ROUTE_TRANSITION_SECONDS = 0.2;
export const DOCUMENT_REVEAL_SECONDS = 0.24;
export const DOCUMENT_STAGGER_SECONDS = 0.04;
