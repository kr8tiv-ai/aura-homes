import Story from "../components/story/Story";

// The scroll-story landing: a small eco home in a boreal clearing, told in
// five beats — land, design, budget, payments, build. Copy renders on the
// server so the exported HTML always carries the full story.
export default function LandingPage() {
  return <Story />;
}
