fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27
svgwrite>=1.4.3

# --- optional, each degrades gracefully if absent ---
ezdxf>=1.3            # DXF export (BSD-3-Clause)
cairosvg>=2.7         # SVG -> PDF (LGPL-3.0; shelled out, not linked)
anthropic>=0.40       # structured reasoning
openai>=1.54          # structured reasoning (alternative)
